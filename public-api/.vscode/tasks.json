{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Go API",
            "type": "shell",
            "command": "go run ./cmd/main.go --port=8000",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Runs the Go API on port 8000"
        }
    ]
}